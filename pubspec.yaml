name: fisiomate
description: "A remote therapeutic monitoring (RTM) mobile app bridging patients and physiotherapists through AI-powered movement tracking"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
